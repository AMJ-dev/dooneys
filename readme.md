UI only. No backend. No APIs.

complete missing pages or pages that is wrong, let every part of the users and admin page be a page on its own, its own route, not just a tab and let the add products, categories, discount codes, access comtrol, staffs and the edit be a page of its own instead of just modal, add forgot password, otp page and admin login and reset password
Add barcode display to the Products admin page with a dialog showing the generated barcode for each product SKU.
Add customer reviews and ratings system to product pages with star ratings and review submission forms.
Add track order page,
View Order details page
instead of image url, let it be an image upload with uploads in category and in product, i can upload up to 5 images and it will be a gallery of images

Add where admin can create promotions and discount codes.
Create a promotions and discount code system that can be applied at checkout.




email: support@akdcu.com
password: 'D:oO>A57)yAIn05%
